<resources>

    <style name="AppThemeAppCompact.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppThemeAppCompact.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="AppThemeAppCompact" parent="AppCompactTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- Base application theme. -->

    <style name="AppCompactTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/red</item>
    </style>
</resources>
